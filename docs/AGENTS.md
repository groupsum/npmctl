# Docs agent guidance

- Keep docs semantically crawlable with descriptive headings.
- Update ADRs/specs when reconciliation behavior changes.
- Keep CLI examples synchronized with the actual parser.
